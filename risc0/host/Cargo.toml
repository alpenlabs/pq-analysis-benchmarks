[package]
name = "host"
version = "0.1.0"
edition = "2021"

[dependencies]
# No `prove` feature: proving uses the installed r0vm binary.
anyhow = "1"
bincode = "1.3"
methods = { workspace = true }
risc0-zkvm = { workspace = true }
serde = "1"
